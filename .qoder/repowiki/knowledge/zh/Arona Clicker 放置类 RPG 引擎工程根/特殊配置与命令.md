`npm run dev` 启动开发服务器并在 5173 端口提供 /game 与 /editor 路由；`npm run build` 输出 web-dist；`npm run test` 同时执行 tests 与 tools 下的单元测试；`npm run gen:schema` 调用 scripts/gen-engine-schema.mjs 生成 datapack editor schema。