修改数据包 schema 后需执行 `npm run gen:schema` 同步生成产物；所有 Phase 验收均以 `npm test`（vitest）+ `npx tsc --noEmit` 全绿为准；禁止直接编辑 `dist/`、`web-dist/`、`src/ui/dist/`、`tools/datapack-editor/schema/engine-defs.gen.json` 等生成目录。