- 前端多应用通过 vite.config.ts 的 Rollup input 集中声明入口 HTML，新增页面需在此注册路由映射。
- 测试文件统一使用 *.test.ts 后缀并按 vitest.config.ts 的 include 规则放置在 tests/ 或 tools/ 下。
- 工具脚本以 .mjs 形式编写并通过 npm scripts 直接 node 调用，不纳入 tsconfig 编译范围。
- 所有 TypeScript 源码遵循 strict 模式并以 ESNext 模块解析，避免在 src 与 tools 之间混用 CommonJS。