`node scripts/gen-engine-schema.mjs --write` 生成 schema JSON；`node scripts/pack-arona-clicker-core.mjs` 打包 AronaClickerCore 数据；`node scripts/_refactor-move-engine.mjs --dry-run` 预览 import 重写，加 `--apply` 实际执行移动与改写；`_fix*-engine-imports.mjs` 系列脚本在中间态阶段运行以修正残留的相对 import。